{
  "name": "skout-ai-backend",
  "version": "0.1.0",
  "private": true,
  "description": "Skout AI backend monorepo — Core API, workers, shared packages",
  "scripts": {
    "dev": "pnpm --filter @skout/api dev",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "prepare": "husky"
  },
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@9.15.0",
  "devDependencies": {
    "husky": "^9.1.7"
  }
}
